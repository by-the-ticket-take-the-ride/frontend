import PopupAuth from "../PopupAuth/PopupAuth";

function Auth(props) {
  
  return (
    <PopupAuth
      // type={props.type}
      // setType={props.setType}
    />
  );
}

export default Auth;
